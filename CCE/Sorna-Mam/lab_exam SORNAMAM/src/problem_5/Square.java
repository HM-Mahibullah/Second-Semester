
package problem_5;
public class Square extends Rectangle {
    public Square(Integer side) {
        // Calling the constructor of the superclass (Rectangle) using 'super'
        super(side, side);
    }
}
